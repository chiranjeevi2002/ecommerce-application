server:
  port: 8081

spring:
  application:
    name: userservice
  output:
    ansi:
      enabled: ALWAYS
  datasource:
#    url: jdbc:h2:mem:usersdb
    url: jdbc:h2:file:D:/workspace/services/ecommerce/ecommerce-backend/main;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
# Replace this with a real base64 key generated by: openssl rand -base64 32
  secret-base64: "vVkNwgLCuunJto86RZveCtNr/HvBEFkpuT13zUNNjhQ="
  expiration-millis: 3600000
  issuer: ecommerce-backend


logging:
  file:
    name: logs/userservice.log
  pattern:
    console: "%clr([%d{yyyy-MM-dd HH:mm:ss}]){faint} %clr([%-5level]){blue} %clr([%X{service}]){green} %clr([%logger{36}:%L]){cyan} - %clr(%msg){bright_white}%n"
    file: "[%d{yyyy-MM-dd HH:mm:ss}] [%-5level] [%X{service}] [%logger{36}:%L] - %msg%n"
  level:
    root: INFO
    com.ecommerce.users: DEBUG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


instance:
    status: DOWN       
    hostname: localhost
    metadata-map:
      description: "Placeholder instance for USERSERVICE"



springdoc:
  swagger-ui:
    enabled: false   # UI disabled at each service
  api-docs:
    path: /v3/api-docs  