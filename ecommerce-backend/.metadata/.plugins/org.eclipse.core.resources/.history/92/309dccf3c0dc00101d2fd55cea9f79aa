server:
  port: 8081

spring:
  application:
    name: userservice

  output:
    ansi:
      enabled: ALWAYS

  datasource:
    url: jdbc:h2:file:D:/workspace/services/ecommerce/ecommerce-backend/main;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# JWT used ONLY for token generation (login),
# NOT for request authentication anymore
jwt:
  secret-base64: "vVkNwgLCuunJto86RZveCtNr/HvBEFkpuT13zUNNjhQ="
  expiration-millis: 3600000
  issuer: ecommerce-backend

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

logging:
  file:
    name: logs/userservice.log
  level:
    root: INFO
    com.ecommerce.userservice: DEBUG

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    path: /v3/api-docs
